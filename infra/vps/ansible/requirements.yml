---
# Pinned, like every other dependency (SR-0.10). Install with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
    version: '12.1.0'
  - name: ansible.posix
    version: '2.1.0'
